<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.user.userSales">
<!-- 판매자가 판매한 전체 상품 -->
<select id="allSales" resultType="kr.co.sist.user.domain.sales.SalesDomain" parameterMap="String">
SELECT PCODE, PNAME,PRICE, INPUT_DATE, WISH, IMG, SALEOK
FROM  PRODUCT
WHERE ID=#{id}
</select>

<!-- 판매자가 판매중인 상품 -->
<select id="onSales" resultType="kr.co.sist.user.domain.sales.SalesDomain" parameterMap="String">
SELECT PCODE, PNAME,PRICE, INPUT_DATE, WISH, IMG, SALEOK
FROM  PRODUCT
WHERE ID=#{id} AND SALEOK='N'
</select>

<!-- 판매자가 판매완료한 상품 -->
<select id="finishedSales" resultType="kr.co.sist.user.domain.sales.SalesDomain" parameterMap="String">
SELECT PCODE, PNAME,PRICE, INPUT_DATE, WISH, IMG, SALEOK
FROM  PRODUCT
WHERE ID=#{id} AND SALEOK='Y'
</select>
</mapper>