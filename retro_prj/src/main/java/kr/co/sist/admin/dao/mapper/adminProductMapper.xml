<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin.product">

<!-- 모든 상품 조회 -->
<select id="selectAllProduct" resultType="admin.domain.AdminProductDomain" parameterType="admin.vo.BoardRangeVO">
select NICKNAME ,NAME, PNAME,PRICE,INPUT_DATE
from(select m.NICKNAME, c2.NAME, p.PNAME, p.PRICE, p.INPUT_DATE
			,row_number() over(order by p.INPUT_DATE desc) rnum
from  PRODUCT p, MEMBER m,CATEGORY2 c2,CATEGORY3 c3
where m.id=p.id   and p.C3CODE = c3.C3CODE and c2.C2CODE=c3.C2CODE 
		<if test="keyword neq null and keyword neq ''">
			<if test="keyword eq 'c2.name'">
			and name like #{ keyword }
			</if>
			<if test="keyword eq 'm.nickname'">
			and nickname like #{ keyword }
			</if>
		</if>
)
 and rnum between 1 and 10
</select>

<!-- 상품 상세 조회 -->
<select id="selectDetailProduct" resultType="apdd" parameterType="String">
select p.PNAME, p.PCANCEL, m.ID, c2.NAME, p.PRICE, p.STATUS,p.INPUT_DATE
from  PRODUCT p, MEMBER m,CATEGORY2 c2,CATEGORY3 c3
where m.id=p.id   and p.C3CODE = c3.C3CODE and c2.C2CODE=c3.C2CODE
and p.pcode=#{ pcode }
</select>

<update id="udpateProduct"  parameterType="admin.vo.AdminProductVO">
update PRODUCT
set   PCANCEL=#{ pcancel }
where  PCODE=#{ pcode }
</update>




</mapper>