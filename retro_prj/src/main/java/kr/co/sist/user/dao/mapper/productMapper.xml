<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user.product">

	<!-- 카테고리 조회 조회 -->
	<insert id="insertProduct" parameterType="pVO">
		insert into PRODUCT (PCODE, PNAME, CONTEXT, IMG, IMG2, IMG3, IMG4,
			 IMG5, PRICE, INPUT_DATE, DELIVER, STATUS, LOC, PCANCEL, C3CODE, ID)
 		values (#{pcode},#{pname},#{context},#{img},#{img2},#{img3},#{img4},
 			#{img5},#{price},sysdate ,#{deliver},#{status},#{loc},'N',
 			#{c3code},#{id})
	</insert>


</mapper>
